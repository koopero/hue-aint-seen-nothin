buffer/analyzer_input:
  format: rgba32

render/analyzer_input:
  src: 
    buffer: spectrum_hls
    filter: linear

render/analyzer:
  shader: analyzer
  src: analyzer_input

show: analyzer

window/title: Hue Ain't Seen Nothin' Yet

window:
  width: 1024
  height: 1024